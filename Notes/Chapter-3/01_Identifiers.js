// Identifier: 
// A name given to a variable, function, or label in JavaScript. 
// Identifiers must follow specific rules and conventions to be valid. 
// They can consist of letters, digits, underscores, and dollar signs, but must not start with a digit.


//let class = "Sri"
//SyntaxError: Unexpected token 'class', because 'class' is a reserved keyword in JavaScript and cannot be used as an identifier (variable name).

//let return = "Hello"
//SyntaxError: Unexpected token 'return', because 'return' is a reserved keyword in JavaScript and cannot be used as an identifier (variable name).

let myVariable = 10; 
// Valid identifier, starts with a letter and contains letters and digits

let undefined = 5;  //works but not recommended
let Infinity = 100; //works but not recommended

// list of reserved keywords in JavaScript:
/* 

break
case
catch
class
const
continue
debugger
default
delete
do
else
export
extends
false
finally
for
function
if
import
in
instanceof
new
null
return
super
switch
this
throw
true
try
typeof
var
void
while
with

*/